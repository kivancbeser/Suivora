import { createCourseAction, updateCourseAction } from "./actions";
import { CourseForm } from "@/features/class-courses/forms";
import type { CourseSummary } from "./data";
import type { StructurePageMessages } from "@/features/class-courses/messages";
export function CoursesPage({courses,m,readError}:{courses:readonly CourseSummary[];m:StructurePageMessages;readError:boolean}) { return <section className="structure-page"><header className="structure-header"><div><p>{m.eyebrow}</p><h1>{m.title}</h1><p>{m.description}</p></div><details><summary>{m.add}</summary><CourseForm action={createCourseAction} m={m.form}/></details></header>{readError?<div className="structure-empty" role="alert"><h2>{m.readErrorTitle}</h2><p>{m.readErrorDescription}</p></div>:courses.length===0?<div className="structure-empty"><h2>{m.emptyTitle}</h2><p>{m.emptyDescription}</p></div>:<div className="structure-list">{courses.map(c=><article className="structure-card" key={c.id}><div><h2>{c.name}</h2>{c.code?<p>{c.code}</p>:null}<span className="structure-status"><span aria-hidden="true">{c.active?"●":"○"}</span> {c.active?m.active:m.inactive}</span></div><details><summary>{m.edit}</summary><CourseForm action={updateCourseAction.bind(null,c.id)} initial={c} m={m.form}/></details></article>)}</div>}</section>; }
