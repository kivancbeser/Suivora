"use server";
import{revalidatePath}from"next/cache";import{createServerSupabaseClient}from"@/lib/supabase/server";import{resolveApplicationContext}from"@/server/application-context";import{parseOutcomeForm,type ProgressionActionState}from"./contracts";
export async function createOutcomeAction(_:ProgressionActionState,data:FormData):Promise<ProgressionActionState>{const parsed=parseOutcomeForm(data);if(!parsed.ok)return{status:"error",message:"invalidFields"};const access=await resolveApplicationContext();if(access.status!=="ready"||access.context.role!=="ADMIN")return{status:"error",message:"accessDenied"};try{const client=await createServerSupabaseClient();const result=await client.rpc("create_learning_outcome",{target_course_id:parsed.data.courseId,outcome_code:parsed.data.code,outcome_title:parsed.data.title,outcome_description:parsed.data.description});if(result.error)return{status:"error",message:result.error.code==="42501"?"accessDenied":"invalidFields"};for(const locale of["fr","tr"])revalidatePath(`/${locale}/app/progression`);return{status:"success",message:"created"};}catch{return{status:"error",message:"unexpected"};}}
