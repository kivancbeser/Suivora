import{notFound,redirect}from"next/navigation";import{getTranslations}from"next-intl/server";import{isLocale}from"@/i18n/routing";import{getMyClassDetail}from"@/features/my-classes/data";import{MyClassDetailPage,type MyClassesMessages}from"@/features/my-classes/pages";
function messages(t:(key:string)=>string):MyClassesMessages{return{eyebrow:t("eyebrow"),title:t("title"),description:t("description"),empty:t("empty"),open:t("open"),classLabel:t("class"),course:t("course"),year:t("year"),allocated:t("allocated"),total:t("total"),back:t("back"),roster:t("roster"),noStudents:t("noStudents"),code:t("code"),current:t("current")};}
export default async function Page({params}:{params:Promise<{locale:string;classCourseId:string}>}){const{locale,classCourseId}=await params;if(!isLocale(locale))notFound();const result=await getMyClassDetail(classCourseId);if(result.status==="unauthenticated")redirect(`/${locale}/connexion`);if(result.status==="forbidden"||result.status==="not-found")notFound();if(result.status==="access-unavailable")return null;const t=await getTranslations({locale,namespace:"MyClasses"});if(result.status==="error")return <section className="structure-empty" role="alert"><h1>{t("error")}</h1></section>;if(result.status!=="ready")notFound();return <MyClassDetailPage locale={locale} m={messages(k=>t(k as never))} value={result.value}/>;}
