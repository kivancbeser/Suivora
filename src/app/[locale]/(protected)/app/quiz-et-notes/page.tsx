import{notFound,redirect}from"next/navigation";import{getTranslations}from"next-intl/server";import{isLocale}from"@/i18n/routing";import{listGradingClasses}from"@/features/grading/data";import{GradingClassesPage}from"@/features/grading/pages";import{makeGradingMessages}from"@/features/grading/messages";
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const result=await listGradingClasses();if(result.status==="unauthenticated")redirect(`/${locale}/connexion`);if(result.status==="access-unavailable")return null;const t=await getTranslations({locale,namespace:"Grading"});if(result.status!=="ready")return <section className="structure-empty" role="alert"><h1>{t("unavailable")}</h1></section>;return <GradingClassesPage classes={result.classes} locale={locale} m={makeGradingMessages(k=>t(k as never))}/>;}
