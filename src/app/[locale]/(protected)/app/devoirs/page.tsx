import{notFound,redirect}from"next/navigation";import{getTranslations}from"next-intl/server";import{isLocale}from"@/i18n/routing";import{listHomeworkClasses}from"@/features/homework/data";import{HomeworkClassesPage}from"@/features/homework/pages";import{makeHomeworkMessages}from"@/features/homework/messages";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const r=await listHomeworkClasses();if(r.status==="unauthenticated")redirect(`/${locale}/connexion`);if(r.status==="access-unavailable")return null;const t=await getTranslations({locale,namespace:"Homework"});if(r.status!=="ready")return<section className="structure-empty" role="alert"><h1>{t("unavailable")}</h1></section>;return<HomeworkClassesPage classes={r.classes} locale={locale} m={makeHomeworkMessages((k,v)=>t(k as never,v as never))}/>;}
